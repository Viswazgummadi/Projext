<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Enigma</title>
    <link rel="icon" type="image/x-icon" href="/static/assets/favicon.ico" />

    <link
      rel="stylesheet"
      href="{{ url_for('static', filename='stylei.css') }}"
    />
    <link
      href="https://unpkg.com/boxicons@2.1.4/css/boxicons.min.css"
      rel="stylesheet"
    />
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/boxicons/2.0.7/css/boxicons.min.css"
    />
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.14.0/css/all.min.css"
    />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
  </head>
  <body>
    <div class="circle"></div>
    <div class="circle"></div>
    <div class="circle"></div>
    <div class="circle"></div>
    <div class="circle"></div>
    <div class="circle"></div>
    <div class="circle"></div>
    <div class="circle"></div>
    <div class="circle"></div>
    <div class="inner-cursor"></div>
    <div class="outer-cursor"></div>
    <div class="header-container">
      <!-- Logo and name -->
      <div class="logo-name">
        <img
          id="logo"
          src="{{ url_for('static', filename='assets/9032132.png') }}"
          alt="logo"
        />
        <h1>Enigma</h1>
      </div>

      <!-- Icons -->
      <div class="header-icons">
        <!-- Icon with tooltip for Favorites -->
        <div class="tooltip">
          <a href="/"><i class="bx bx-home-smile"></i></a>
          <span class="tooltiptext">Home</span>
        </div>

        <div class="tooltip">
          <a href="sugg.html"><i class="bx bx-images"></i></a>
          <span class="tooltiptext">Suggestions</span>
        </div>

        <div class="tooltip">
          <a href="favs.html"><i class="bx bx-book-heart"></i></a>
          <span class="tooltiptext">Favorites</span>
        </div>

        <!-- Icon with tooltip for Cart -->
        <div class="tooltip">
          <a href="cart.html"><i class="bx bxs-cart"></i></a>
          <span class="tooltiptext">Cart</span>
        </div>

        <!-- Icon with tooltip for Work -->
        <div class="tooltip">
          <a href="work.html"><i class="bx bxs-network-chart"></i></a>
          <span class="tooltiptext">Work</span>
        </div>

        <!-- Icon with tooltip for Crew -->
        <div class="tooltip">
          <a href="crew.html"><i class="bx bxs-id-card"></i></a>
          <span class="tooltiptext">Crew</span>
        </div>

        <div class="tooltip">
          <a href="dev.html" class="active"><i class="bx bxl-dev-to"></i></a>
          <span class="tooltiptext">Dev</span>
        </div>
      </div>
    </div>
    <div class="status">
      <i class="bx bxs-radiation"></i> Caution!! brains at work ...
    </div>
    <script>
      document.addEventListener("DOMContentLoaded", function () {
        // Get the inner and outer cursors
        let innerCursor = document.querySelector(".inner-cursor");

        // Update the cursor position on mouse move
        document.addEventListener("mousemove", mouseCursor);

        function mouseCursor(e) {
          let x = e.clientX;
          let y = e.clientY;

          // Update the position of the cursors
          innerCursor.style.left = `${x}px`;
          innerCursor.style.top = `${y}px`;
          outerCursor.style.left = `${x}px`;
          outerCursor.style.top = `${y}px`;
        }

        // Increase the size of the inner cursor on hover over clickable elements
        let clickableElements = document.querySelectorAll(
          "a, button, input[type='submit'], input[type='file'], label[for='file-input']"
        );

        clickableElements.forEach((element) => {
          element.addEventListener("mouseover", () => {
            innerCursor.classList.add("grow");
          });

          element.addEventListener("mouseleave", () => {
            innerCursor.classList.remove("grow");
          });
        });

        // Get the circles and set initial coordinates
        const coords = { x: 0, y: 0 };
        const circles = document.querySelectorAll(".circle");
        const colors = [
          "#1f005c",
          "#1d146c",
          "#19247c",
          "#12348b",
          "#044299",
          "#0051a7",
          "#0060b5",
          "#006fc2",
          "#007ece",
          "#008dda",
          "#009ce5",
          "#00abf0",
        ];
        circles.forEach(function (circle, index) {
          circle.x = 0;
          circle.y = 0;
          circle.style.backgroundColor = colors[index % colors.length];
        });

        // Update the coordinates on mouse move
        window.addEventListener("mousemove", function (e) {
          coords.x = e.clientX;
          coords.y = e.clientY;
        });

        // Animate the circles
        function animateCircles() {
          let x = coords.x;
          let y = coords.y;

          circles.forEach(function (circle, index) {
            // Update the position and size of the circle
            circle.style.left = x - circle.offsetWidth / 2 + "px";
            circle.style.top = y - circle.offsetHeight / 2 + "px";
            circle.style.transform = `scale(${
              (circles.length - index) / circles.length
            })`;

            circle.x = x;
            circle.y = y;

            // Update the next circle's position
            const nextCircle = circles[index + 1] || circles[0];
            x += (nextCircle.x - x) * 0.12;
            y += (nextCircle.y - y) * 0.12;
          });

          requestAnimationFrame(animateCircles);
        }

        animateCircles();
      });
    </script>
  </body>
</html>


@import url("https://fonts.googleapis.com/css2?family=Poppins:wght@200;400;500;600;700;800;900%display=swap");
@import url("https://fonts.googleapis.com/css2?family=Do+Hyeon&family=Press+Start+2P&display=swap");
body {
  background-color: rgb(116, 196, 223);
  font-family: Arial, sans-serif;
}
/* Style for the active link */
.header-icons .tooltip a.active {
  text-decoration: underline; /* Add underline to the active link */
}

.status {
  color: black;
  position: absolute;
  bottom: 50%;
  right: 22%;
  font-family: "Do Hyeon", sans-serif;
  font-family: "Press Start 2P", cursive;
  font-size: 28px;
}
.status i {
  zoom: 200%;
}
* {
  cursor: none;
}

h1 {
  color: rgb(0, 0, 0);
}

.image-container {
  position: relative;
  display: flex;
  justify-content: center;
  align-items: center;
  margin: 0 auto; /* Center the container */
  max-width: 600px;
}
.image-container {
  position: relative;
  max-width: 600px;
  margin: 0 auto;
}

.image-wrapper {
  position: relative;
}

.image-wrapper img {
  max-width: 100%;
  max-height: 75vh;
  border-radius: 13px;
}

.action-icons {
  position: absolute;
  padding-top: 20px; /* Adjusted padding */
  bottom: 20px; /* Adjusted bottom spacing */
  left: 50%;
  transform: translateX(-50%);
  display: flex;
  justify-content: center; /* Center the icons horizontally */
  gap: 10px; /* Reduced gap between icons */
}

.action-icons i {
  font-size: 24px;
}

.prev,
.next {
  position: absolute;
  top: 50%;
  transform: translateY(-50%);
  font-size: 24px;
  padding: 10px;
  background-color: rgba(255, 255, 255, 0.5);
  border: none;
  border-radius: 20px;
  outline: none;
}
.prev {
  left: 10px;
}
.next {
  right: 10px;
}
.description {
  font-size: 16px;
  line-height: 1.5;
}
::-webkit-scrollbar {
  width: 10px; /* Width of the scrollbar */
}
::-webkit-scrollbar-track {
  background: #17586b00; /* Background color of the track */
}
::-webkit-scrollbar-thumb {
  background: rgba(6, 86, 161, 0.381);
  border-radius: 5px; /* Roundness of the thumb */
}
::-webkit-scrollbar-thumb:hover {
  background: rgba(6, 86, 161, 0.732); /* Color of the thumb on hover */
}
.card-container {
  display: flex;
  justify-content: space-between;
  margin: 20px;
  z-index: -2;
}

/* Styles for individual cards */
.card {
  background-color: #fff;
  border-radius: 15px;
  box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
  padding: 20px;
  margin: 20px;
  width: calc(33.33% - 20px); /* Adjust width to fit three cards side by side */
  z-index: -2;
}

/* Styles for card content */
.card-content {
  font-size: 16px;
  line-height: 1.5;
}
.header-container {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 20px;
  background-color: #f0f0f000;
}

/* Styles for logo */
#logo {
  width: 50px;
  height: auto;
  margin-right: 20px;
}

/* Styles for name */
h1 {
  margin: 0;
  font-size: 24px;
}

/* Styles for icons */
.header-icons {
  display: flex;
  align-items: center;
}

.header-icons i {
  margin-right: 10px;
  padding-right: 40px;
  color: black; /* Use the default text color */
  text-decoration: none; /* Remove underline */
  font-size: 30px;
}

/* Styles for tooltips */
.tooltip {
  position: relative;
  display: inline-block;
}

.tooltip .tooltiptext {
  visibility: hidden;
  width: 120px;
  background-color: #333;
  color: #fff;
  text-align: center;
  border-radius: 6px;
  padding: 5px;
  position: absolute;
  top: -30px;
  left: 50%;
  transform: translateX(-50%);
  opacity: 0;
  transition: opacity 0.3s;
}

.tooltip:hover .tooltiptext {
  visibility: visible;
  opacity: 1;
}

.logo-name {
  display: flex;
  align-items: center; /* Vertically align items */
  margin-bottom: 20px;
}
.card-container2 {
  display: flex;
  flex-wrap: wrap;
  justify-content: space-between;
  padding: 20px;
  z-index: -2;
}

/* Styles for card */
.card2 {
  width: calc(25% - 10px);
  margin-bottom: 20px;
  background-color: #f0f0f0;
  border-radius: 10px;
  padding: 10px;
  box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
  text-align: center;
  box-sizing: border-box; /* Add this property to include padding in width calculation */
  z-index: -2;
}

/* Styles for second row cards */
.card-container2 .card2:nth-child(n + 5) {
  width: calc(33.33% - 10px); /* Set width for second row cards */
}

#image-upload {
  text-align: center;
}

#file-input {
  display: none;
}

#file-label {
  display: inline-block;
  border: 1px solid black; /* Added a border for visibility */
  color: black;
  padding: 10px 20px;
  border-radius: 5px;
  transition: background-color 0.3s ease;
}

#file-label:hover {
  background-color: #297fb96f;
}

#drop-area {
  border: 2px dashed #ccc;
  padding: 20px;
  margin-top: 20px;
  border-radius: 5px;
}

#drop-area p {
  margin: 0;
}
#upload-form button {
  margin-top: 10px;
  border: 1px solid black; /* Added a border for visibility */
  background-color: transparent;
  color: black;
  padding: 10px 20px;
  border-radius: 5px;
  transition: background-color 0.3s ease;
}

#upload-form button:hover {
  background-color: #297fb98c;
}

.circle {
  position: absolute;
  height: 14px;
  width: 14px;
  border-radius: 24px;
  background-color: #00acf0;
  z-index: -1;
}
.inner-cursor {
  position: absolute;
  left: 0;
  top: 0;
  width: 12px;
  height: 12px;
  transform: translate(-50%, -50%);
  background-color: #00305c;
  mix-blend-mode: normal;
  border-radius: 50%;
  transition: width 0.5s, height 0.5s;
  z-index: -1;
}

.inner-cursor.grow {
  width: 30px;
  height: 30px;
  transition: width 0.5s, height 0.5s;
}
